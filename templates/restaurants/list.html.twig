{% extends 'base.html.twig' %}

{% block title %}Ajouter un restaurant {% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <div class="container">
        <div class="row">

            <h1>Liste des modèles</h1>
            <table class="table table-hover">
                <tr>
                    <td>id</td>
                    <td>Libelle</td>
                    <td>speciality</td>
                    <td>telephone</td>
                    <td>Supprimer</td>
                    <td>Modifier</td>
                </tr>
                {% for resto in restau %}
                    <tr>
                        <td>{{ resto.idResto }}</td>
                        <td>{{ resto.libelle }}</td>
                        <td>{{ resto.specialite}}</td>
                        <td>{{ resto.tel}}</td>
                        <td><a href="{{ path("delete_resto", {'id' : resto.idResto}) }}" >Supprimer</a> </td>
                        <td><a href="{{ path("update_resto", {'id' : resto.idResto}) }}" >Modifier</a> </td>
                    </tr>
                {% endfor %}

            </table>

    </div>
    </div>
{% endblock %}
