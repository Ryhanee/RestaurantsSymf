{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter un restaurant {% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <div class="container">
        <div class="row">
            <div class="col-10">
            <h1>Liste des restaurants</h1>
            </div>
            <div class="col-2 ajout"><a href="{{ path('add_resto') }}">Ajouter</a> </div>
            <table class="table table-hover">
                <th>
                <tr>

                    <td>id</td>
                    <td>Libelle</td>
                    <td>Spécialité</td>
                    <td>Téléphone</td>
                    <td></td>
                    <td></td>

                </tr>
                </th>
                {% for resto in restau %}
                    <tr>
                        <td><a href="{{ path('show_resto_admin', {'libelle':resto.libelle}) }}">{{ resto.id }}</a></td>
                        <td>{{ resto.libelle }}</td>
                        <td>{{ resto.specialite}}</td>
                        <td>{{ resto.tel}}</td>
                        <td><a href="{{ path("delete_resto", {'id' : resto.id}) }}" >Supprimer</a> </td>
                        <td><a href="{{ path("update_resto", {'id' : resto.id}) }}" >Modifier</a> </td>
                    </tr>
                {% endfor %}

            </table>

    </div>
    </div>
{% endblock %}
